# =============================================================================
# Vestra ERP — Variáveis de ambiente
# Copie para .env.local e preencha. NUNCA commite .env.local (já está no .gitignore).
# =============================================================================

# --- PÚBLICAS (vão para o cliente: web e desktop) ---
# Qualquer NEXT_PUBLIC_* é incluído no bundle e visível para o usuário.
NEXT_PUBLIC_APP_URL=https://vestra.app.br
NEXT_PUBLIC_SUPABASE_URL=https://seu-projeto.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJ...

# --- SECRETAS (apenas servidor; NUNCA use NEXT_PUBLIC_ para estas) ---
DATABASE_URL=postgresql://...
SUPABASE_SERVICE_ROLE_KEY=eyJ...

# Asaas (cobrança)
ASAAS_API_KEY=$aact_...
ASAAS_ENV=sandbox
ASAAS_WEBHOOK_TOKEN=seu-token-webhook

# Focus NFe (notas fiscais)
FOCUS_ENV=homologation
FOCUS_NFE_TOKEN=...
FOCUS_NFE_TOKEN_HOMOLOG=...
FOCUS_NFE_TOKEN_PROD=...

# Admin da plataforma (emails separados por vírgula)
VESTRA_ADMIN_EMAILS=admin@example.com,outro@example.com

# --- Atualizações do app desktop (Tauri) ---
# Versão que o servidor anuncia (ex.: 0.2.0). Cliente compara com a versão instalada.
TAURI_UPDATE_VERSION=0.1.0
# URL do instalador por plataforma (ex.: Windows x64)
# TAURI_UPDATE_URL_WINDOWS_X86_64=https://vestra.app.br/downloads/Vestra_0.2.0_x64-setup.nsis.zip
# Assinatura do instalador (gerada no build com TAURI_SIGNING_PRIVATE_KEY)
# TAURI_UPDATE_SIGNATURE_WINDOWS_X86_64=...
